# Module to train and record an expert trajectory.
#[train_expert]
output_dir = "experiments/sanity_check/checkpoints/r32p_4xl/0/"
model = "r32p"
trainer = "sgd"
dataset = "cifar"
source_label = 9
target_label = 4
poisoner = "4xl"
epochs = 20
checkpoint_iters = 50

# Module to generate attack labels from the expert trajectories.
[generate_labels]
input_pths = "out/checkpoints/r32p_1xs/{}/model_{}_{}.pth"
opt_pths = "out/checkpoints/r32p_1xs/{}/model_{}_{}_opt.pth"
expert_model = "r32p"
trainer = "sgd"
dataset = "cifar"
source_label = 9
target_label = 4
poisoner = "4xl"
output_dir = "experiments/sanity_check/"
lambda = 0.0
num_honests = 0
num_poisoned = 1

# Module to flip labels at the provided budgets.
[select_flips]
budgets = [150, 300, 500, 1000, 1500]
input_label_glob = "experiments/sanity_check/labels.npy"
true_labels = "experiments/sanity_check/true.npy"
output_dir = "experiments/sanity_check/"
num_honests = 0
num_poisoned = 1
